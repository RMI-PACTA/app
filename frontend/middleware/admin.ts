export default defineNuxtRouteMiddleware(async () => {
  /* TODO(grady) add Authorization here
  const { $msal: { getMe } } = useNuxtApp()
  const { isAdmin } = await getMe()
  if (!isAdmin.value) {
    return await navigateTo('/denied')
  }
  */
})
